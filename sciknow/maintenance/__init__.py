"""Phase 52 — maintenance utilities.

Lives alongside `sciknow/ingestion` and `sciknow/retrieval` so the CLI
can cleanly expose ``sciknow db dedup`` / ``sciknow db repair``
without crowding the ingest module.
"""
